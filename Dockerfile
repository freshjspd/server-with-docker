# Базируемся на готовом имейдже node:20.5.1-alpine3.18
FROM node:20.5.1-alpine3.18
# Создаем папку для хранения файлов сервера
RUN mkdir server
# Назначаем рабочей папкой /server
WORKDIR /server
# Скопировать все файлы в рабочую папку
COPY . .
# Установить все пакеты
RUN npm i
# Задекларировать порт
EXPOSE 5000
# Запустить сервер при запуске контейнера
CMD npm run dev